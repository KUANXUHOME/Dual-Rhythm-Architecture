# 2026-07-04 工作日志

## 第六轮：产品审查 + 物理公式暴露风险审计

### 产出
- [x] 创建 `/Users/kuanxu/Desktop/dra-v2/audit-report.html` — 10 章完整审查报告

### 关键发现
1. **CRITICAL — OSS 公式暴露在前端**：`TheOSSIndexResultInline.tsx`（'use client'）直接 import `calculateTheOSSIndex`，导致完整公式（SSC=cbrt(er*pr*ri)、HP/LR 阈值、风险区边界、推荐逻辑）全部打包进浏览器 JS bundle。竞争对手可通过 DevTools Sources 提取。
   - 修复方案：engine.ts 加 `import 'server-only'` + 新建 `/api/oss-result` 路由 + 重写 Inline 组件为 fetch 展示数据
2. PDF 生成流程已验证（4 个触发点全部服务端，公式不暴露）
3. 5 层闭环已验证全部产品化（Layer 3 有暴露风险）
4. 遗漏 10 项（2 critical + 4 medium + 4 low）
5. 真实变现产品 6 档（5 live + 1 not built）+ 5 工作流
6. AI agent 1 个（5 工作流模式），PDF/CEO Brief/Board Rhythm 非 agent
7. 自定义模型入口：当前 8 模型可选但无 BYOK，建议 Phase 2
8. Web SaaS vs 桌面：当前是 Web SaaS，Tauri 包装需 3 天（Phase 2）
9. 运维成本：100 用户约 $100/mo，gross margin >95%
10. 盈利：首个付费用户即盈利，12 月保守 $280K

### 用户待操作（优先级排序）
1. **CRITICAL**: 修复公式暴露（加 server-only + API route）
2. **CRITICAL**: 填 Stripe webhook secret（否则支付不验证）
3. **MEDIUM**: 加季度 cron（订阅留存）

## 第五轮：本地测试问题修复 + 多模型 + Agent 工作流变现

### 问题 1 — 登录状态显示（修复）
- [x] 根因：layout.tsx 结构错误导致 Clerk 无法初始化（providers 包裹 html 而非在 body 内）
- [x] 修复 app/layout.tsx：html 在最外层 + suppressHydrationWarning，providers 移入 body
- [x] 新建 components/providers/AppProviders.tsx（Client Component 封装 ClerkProvider + ThemeProvider + Toaster）
- [x] AgentSidebar 增强：显示用户 email + UserButton 并排

### 问题 2 — 多模型选择器 + NVIDIA API（修复）
- [x] 重写 lib/ai-provider.ts：MODEL_REGISTRY（8 个模型：DeepSeek 2 + NVIDIA NIM 6）
  - DeepSeek Chat (free) / DeepSeek Reasoner R1 (paid)
  - Llama 3.3 70B (free) / Llama 3.1 405B (paid) / Nemotron 70B (free)
  - Llama 3.1 70B (free) / Mistral Large 2 (paid) / Qwen 2.5 72B (free)
- [x] getModelById(modelId) 运行时模型解析 + tier 校验
- [x] 新建 components/chat/ModelSelector.tsx：下拉选择器，按 provider 分组，paid 模型锁定显示 Lock 图标
- [x] /api/chat GET 端点：返回用户可用模型列表 + isPaid 状态
- [x] /api/chat POST 扩展：body.modelId + body.workflowId + tier 校验（paid 模型需付费）

### 问题 2 — Agent 工作流变现（新增）
- [x] 新建 lib/workflows.ts：5 个工作流定义
  - Stability Diagnostic (free) — 基础 OSS 诊断
  - Quarterly Board Review (paid) — 季度对比 + 趋势告警
  - M&A Due Diligence (paid) — 并购尽调 + 整合风险
  - Crisis Response (paid) — 危机响应 + 48h 稳定协议
  - Team Restructuring (paid) — 重组影响预测 + 节奏保护
- [x] 新建 components/chat/WorkflowSelector.tsx：工作流选择器 UI
- [x] /api/chat 集成 workflowId：系统提示追加 workflow-specific 指令 + tier 校验
- [x] ChatInterface 集成：顶部工具栏显示 WorkflowSelector + ModelSelector

### 问题 3+4 — Console 错误（修复）
- [x] "Cannot render sync/defer script outside main document" — layout 结构修复（providers 移入 body）
- [x] Hydration mismatch (className="light") — html 加 suppressHydrationWarning + ThemeProvider 加 disableTransitionOnChange

### 问题 5 — Supabase SQL 警告（修复）
- [x] 001_init.sql：_update_updated_at + increment_usage 加 `SET search_path = public`
- [x] 新建 006_fix_search_path.sql：用于已跑过旧迁移的数据库修复

### 问题 6+7 — 环境变量清理（修复）
- [x] .env.local 清理：旧 NVIDIA API key（已暴露）替换为 REPLACE_ME
- [x] 新 Clerk/Supabase key 保留（用户已填入新账号值）
- [x] JWKS_URL 修正为匹配新 Supabase URL
- [x] NEXT_PUBLIC_APP_URL 改为 localhost:3000（本地开发用）

### 最终验证
- [x] npm audit: 0 vulnerabilities
- [x] npm run lint: 0 errors / 0 warnings
- [x] npm run build: 成功，23 路由

### 用户待操作
1. Clerk Dashboard 配 Allowed Origins: http://localhost:3000
2. 填 SUPABASE_PUBLISHABLE_KEY（从新 Supabase Dashboard 获取）
3. 填新 NVIDIA_API_KEY（从新 NVIDIA 账号获取）
4. 填 Stripe/DeepSeek/Resend/Admin 其余占位符
5. 在新 Supabase SQL Editor 执行 006_fix_search_path.sql（消除已存在的警告）
